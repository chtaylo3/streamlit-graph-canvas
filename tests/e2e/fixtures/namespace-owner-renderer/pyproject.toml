[build-system]
requires = ["uv_build>=0.12.6,<0.13"]
build-backend = "uv_build"

[project]
name = "scg-namespace-owner-fixture"
version = "0.1.0"
description = "Owned shared-namespace leaf used by hostile provenance tests."
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.12"
dependencies = ["streamlit-graph-canvas>=0.1.0.dev0,<0.2"]

[tool.uv.build-backend]
module-name = "scg_shared_namespace"
namespace = true
