{
  "name": "streamlit-graph-canvas-conformance",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "test": "playwright test",
    "test:update": "playwright test --update-snapshots",
    "test:features": "playwright test --config playwright.budget.config.ts && playwright test --config playwright.search.config.ts && playwright test --config playwright.label.config.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.52.0",
    "@types/node": "^26.5.0",
    "typescript": "^7.0.2"
  }
}
